import java.util.*;
import java.io.*;
public class palindromeTest {
    public static void main(String[]args)throws IOException {
    Scanner in = new Scanner(new FileReader("palindromewords.txt"));
    PrintWriter out = new PrintWriter(new FileWriter("palindromes.txt"));
    String []phrase = new String[100];
    String []palindromes = new String[50];
    String m;
    String s="END";
    while (phrase!=s){
    m =isPalindrome(phrase);
    }
    out.printf("\nPalindromes are :%s\n ",m);
    in.close();
    out.close();
    }
 public static boolean isPalindrome(String phrase) {
    int left  = 0;
    int right = phrase.length() -1;

    while (left < right) {
        if (phrase.charAt(left) != phrase.charAt(right)) {
            return false;
        }
        left++;
        right--;
    }
    return true;
}

}


